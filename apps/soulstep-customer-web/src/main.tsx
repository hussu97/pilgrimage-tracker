/**
 * @deprecated Vite entry point — no longer used after migration to Next.js.
 * The application entry point is now app/layout.tsx (Next.js App Router).
 * This file is retained only to avoid breaking any CI scripts that reference it.
 */
export {};
